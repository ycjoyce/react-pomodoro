import styled from "styled-components";
import StyledPanelList from "../PanelList/PanelList.style";

const StyledPanel = styled.div`
  & ${StyledPanelList} {
    margin: 0;
    padding: 0;uu
  }
`;

export default StyledPanel;
