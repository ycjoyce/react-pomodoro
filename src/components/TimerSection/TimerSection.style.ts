import styled from "styled-components";
import { mobileLandscape } from "../../styles/abstracts/mixin";

const StyledTimerSection = styled.div`
  ${mobileLandscape} {
    height: 100%;
  }
`;

export default StyledTimerSection;
